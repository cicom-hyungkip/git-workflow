{
  "name": "git-workflow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "distr": "git checkout vendor1 && git pull origin vendor1 && git pull origin master && git push origin vendor1 -f && git checkout vendor2 && git pull origin vendor2 && git pull origin master && git push origin vendor2 -f
    && git checkout vendor1-dev && git pull origin vendor1-dev && git pull origin vendor1 && git push origin vendor1-dev -f && git checkout vendor2-dev && git pull origin vendor2-dev && git pull origin vendor2 && git push origin vendor2-dev -f"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cicom-hyungkip/git-workflow.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cicom-hyungkip/git-workflow/issues"
  },
  "homepage": "https://github.com/cicom-hyungkip/git-workflow#readme"
}
